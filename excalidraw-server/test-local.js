// Quick test of the server locally
const server = require('./server.js');

console.log('Testing server locally...');

// Server will start automatically when required
setTimeout(() => {
  console.log('\nTest complete. Press Ctrl+C to exit.');
}, 2000);